IMAGE=quay.io/jduimovich0/bootstrap

docker.exe build . -t $IMAGE
 
NM=stub
docker.exe stop $NM
docker.exe rm $NM
docker.exe run -d --name $NM -p 80:8080 --env GIT_REPO=XYZ $IMAGE 
docker.exe logs -f  $NM 